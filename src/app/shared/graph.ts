
//struttura per salvare il grafo e dialogare con le API
export class Graph {

    graph   : Object; // struttura json che contienen nodi e archi
    id      : string; // id univoco del grafo
    name    : string; // nome del grafo
    //SI POTREBBE METTERE QUI CONFIG?
    //in questo modo ogni grafo avrebbe la sua personalizzazione di frontend
}
